(function() {
  'use strict';

  angular
    .module('zideen')
    .controller('MainController', MainController);

  /** @ngInject */
  function MainController($scope, util) {
      util.extend($scope, {
          
      });
      
  }
})();
